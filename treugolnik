#include <iostream>
#include <iomanip>
#define _USE_MATH_DEFINES
#include <math.h>
using namespace std;

int main()
{
	int a, b, c;
	cin >> a >> b >> c;
	double p = double (a + b + c) / 2;
	double s = sqrt(p * (p - a) * (p - b) * (p - c));
	cout << s;
}
