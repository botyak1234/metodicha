
#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <algorithm>
#include <vector>
#include <string>
#include <cstring>
using namespace std;



int main()
{
	int c = 0;
	string str;
	int flag = 1;
	getline(cin, str);
	string bukva; cin >> bukva;
	for (int i = 0; i < size(str); i++)
	{
		string symbol; symbol.insert(0, 1, str[i]);
		if (symbol == bukva && str[i - 1] == ' ')
		{
			while (str[i] != ' ')
			{
				cout << str[i];
				i++;
			}
			cout << endl;
		}
	}
}
