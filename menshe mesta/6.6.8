#include <iostream>
#include <cmath>
#include <iomanip>



using namespace std;
int main()
{
	int n; cin >> n;
	int size = n;
	int c = 0;
	int chislo; cin >> chislo;
	long** a = new long* [n];
	for (int i = 0; i < n; i++)
	{
		a[i] = new long[n];
	}
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < n; j++)
		{
			cin >> a[i][j];
		}
	}
	cout << endl;
	cout << endl;
	cout << endl;
	for (int i = 0; i < n; i++)
	{
		int sum = 0;
		for (int j = 0; j < n; j++)
		{
			sum += a[i][j];
		}
		if (sum <= chislo)
		{
		    c++;
			for (int j = i; j < n - 1; j++)
			{
				a[j] = a[j + 1];
				i--;
			}
		}
	}
	for (int i = 0; i < n-c; i++)
	{
		for (int j = 0; j < size; j++)
		{
			cout << a[i][j] << ' ';
		}
		cout << endl;
	}
	for (int i = 0; i < n; i++)
		{
			delete [] a[i];
		}
	delete []a;
