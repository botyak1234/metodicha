




#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <vector>
#include <string>
using namespace std;



int main()
{
	int n = 0; cin >> n;
	int elem; cin >> elem;
	long *a = new long [n+1];
	long* b= new long[n];
	int c = 0;
	int ind;
	int flag = 0;
	for (int i = 0; i < n; i++)
	{
		cin >> a[i];
		if (a[i] < 0 and flag == 0)
		{
			ind = i;
			flag = 1;
		}
	}
	for (int i = n; i > ind; i--)
	{
		a[i] = a[i - 1];
	}
	a[ind] = elem;
	for (int i = 0; i < n + 1; i++)
	{
		cout << a[i] << ' ';
	}
}
