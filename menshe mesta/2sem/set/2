#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <algorithm>
#include <vector>
#include <string>
#include <sstream>
#include <cstring>
#include <fstream>
#include <stack>
#include "queue.cpp"
#include "list.cpp"
#include <list>
#include <set>
using namespace std;


int main()
{
	int n; cin >> n;
	string str;
	multiset<int> s;
	set<int>ss;
	vector<string> array;
	for (int i = 0; i < n; i++)
	{
		cin >> str;
		for (char c : str)
		{
			if (isdigit(c)) ss.insert(c-'0');
		}
		for (auto it = ss.begin(); it != ss.end(); ++it)
		{
			int x = *it;
			s.insert(x);
		}
		ss.clear();
	}
	for (auto it = s.begin(); it != s.end(); ++it)
	{
		int x = *it;
		if (s.count(x) == n)
		{
			cout << x << ' ';
			for (int i = 0; i < n; i++) ++it;
		}
	}
		/*for (int i = 0; i < size(ss); i++)
		{
			int x = *ss.begin() + i;
			s.insert(x);
		}
	}
	for (int i = 0; i < size(s); i++)
	{
		int x = *s.begin() + i;
		if (count(s.begin(), s.end(), x) == n) cout << x << ' ';
	}*/
}
