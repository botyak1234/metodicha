#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;



int main()
{
	char c1; cin >> c1;
	char c2; cin >> c2;
	string s; cin >> s;
	char zabyl = s[size(s) - 1];
	int c = count(s.begin(), s.end(), c2);
	s.insert(size(s), c, '1');
	int lens = size(s);
	/*cout << s << endl;*/
	for (int i = 0; i < lens; i++)
	{
		if (s[i] == c2)
		{
			for (int j = lens-1; j > i+1; j--)
			{
				s[j] = s[j - 1];
			}
			s[i + 1] = c1;
			if (c1 == c2) i++;
		}
	}
	cout << s;
}
