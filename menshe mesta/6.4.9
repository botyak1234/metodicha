#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <vector>
#include <string>
using namespace std;



int main()
{
	int flag = 0;
	int fl = 0;
	long n; cin >> n;
	long chislo; cin >> chislo;
	long **b = new long *[n];
	long* a = new long[n];
	for (long i = 0; i < n; i++)
		b[i] = new long[n];
	for (long i = 0; i < n; i++)
	{
		for (long j = 0; j < n; j++)
		{
			cin >> b[i][j];
		}
	}
	for (long i = 0; i < n; i++)
	{
		long c = 0;
		for (long j = 0; j < n; j++)
		{
			if (b[i][j] > chislo)
			{
				c++;
			}
		}
		a[i] = c;
	}
	for (long i = 0; i < n; i++)
		cout << a[i] << endl;
	delete []a;
	for (long i = 0; i < n; i++) delete [] b[i];
	delete []b;
}
/*
ввод
4
5
1 2 3 4
6 0 1 7
11 4 4 4
9 9 9 9
вывод
0 2 1 4
*/
