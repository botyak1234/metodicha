#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <algorithm>
#include <vector>
#include <string>
#include <cstring>
#include <fstream>
using namespace std;



int main()
{
	ifstream in("input.txt");
	ofstream out("output.txt");
	string s;
	int mxdlina = 0;
	string symb; cin >> symb;
	string pred;
	string tek;
	in >> s;
	cout << s << '*' << endl;
	pred = s;
	while (in.peek() != EOF)
	{
		in >> s;
		cout << s  << '*' << endl;
		tek = s;
		cout << pred << ' ' << tek << endl;
		if (pred == symb) out << tek << ' ';
		pred = tek;
	}
}
