#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <vector>
#include <string>
using namespace std;







//динамический двумерный
// int main()
//{
//	long n; cin >> n;
//	long** a = new long* [n];//объявление двумерного динамического массива
//	for (int i = 0; i < n; i++)
//	{
//		long *a = new long[n];//выделение памяти под строки
//	}
//	for (int i = 0; i < n; i++)
//	{
//		for (int j = 0; j < n; j++)
//		{
//			cin >> a[i][j];//ввод элементов 
//		}
//	}
//	for (int i = 0; i < n; i++)
//	{
//		for (int j = 0; j < n; j++)
//		{
//			if (abs(a[i][j]) % 2 != 0)
//			{
//				cout << i + 1 + j * n;
//			}
//		}
//	}
//	for (int i = 0; i < n; i++)
//	{
//		delete [] a[i];
//	}
//	delete a[i];
//}








//
// статический двумерный
// int main()
//{
//	long a[100][100];//объявление двумерного статического массива
//	long n; cin >> n;
//	for (int i = 0; i < n; i++)
//	{
//		for (int j = 0; j < n; j++)
//		{
//			cin >> a[i][j];//ввод элементов массива
//		}
//	}
//	for (int i = 0; i < n; i++)
//	{
//		for (int j = 0; j < n; j++)
//		{
//			if (abs(a[i][j]) % 2 != 0)
//			{
//				cout << i + 1 + j * n;
//			}
//		}
//	}
//}

//одномерный статический
//int main()
//{
//	long a[10000];//объявление одномерного статического массива 
//	long n; cin >> n;
//	for (int i = 0; i < n; i++)
//	{
//		cin >> a[i];//ввод элементов
//	}
//	for (int i = 0; i < n; i++)
//	{
//		if (abs(a[i]) % 2 != 0)
//			cout << i + 1 << ' ';
//	}
//}





/* одномерный динамический
int main()
{
	
	long n; cin >> n;
	long *a = new long[n];//объявление динамического одномерного массива
	for (int i = 0; i < n; i++)
	{
		cin >> a[i];//ввод элементов
	}
	for (int i = 0; i < n; i++)
	{
		if (abs(a[i]) % 2 != 0)
			cout << i + 1 << ' ';
	}
	delete a[i[;
}
*/
