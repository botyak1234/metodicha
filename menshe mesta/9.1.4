#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <algorithm>
#include <vector>
#include <string>
#include <cstring>
#include <fstream>
using namespace std;



int main()
{
	ifstream in("input.txt");
	ofstream out("output.txt");
	string s;
	int mxdlina = 0;
	while (in.peek() != EOF)
	{
		int dlina;
		getline(in, s);
		dlina = size(s);
		if (dlina > mxdlina) mxdlina = dlina;
	}
	out << mxdlina;
} 
