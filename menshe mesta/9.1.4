#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <algorithm>
#include <vector>
#include <string>
#include <cstring>
#include <fstream>
using namespace std;



int main()
{
	ifstream in("input.txt");//объявление входного потока и сваязывание его с файлом
	ofstream out("output.txt");//объявление выходного потока и связывание его с файлом
	string s;
	string vihod;
	long mndlina = 1000000000;
	while (in.peek() != EOF)//пока не конец файла
	{
		int dlina;
		getline(in, s);
		dlina = size(s);
		if (dlina < mndlina)
		{
			mndlina = dlina;
			vihod = s;
		}
	}
	out << mndlina << '\n';//запись в файл
	out << vihod;
	in.close();//закрытие файлов ввода и вывода
	out.close();
}
/*
ввод
i love watermelon
i love anime
dreams
вывод
6
dreams
*/



