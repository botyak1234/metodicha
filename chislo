#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

string chislo(double a)
{
	a = -a;
	return (a > 0 ? "true" : "false");
}


int main()
{
	double x;
	cin >> x;
	cout << chislo(x);
} 
