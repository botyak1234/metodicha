#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
using namespace std;



int main()
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	double itog = 0;
	for (int x = 2; x <= 12; x += 2)
	{
		itog = x * 25.4 / 10;
		cout << x << " дюймов - " << itog << " сантиметров" << endl;
	}
}

/*
2 дюймов - 5.08 сантиметров
4 дюймов - 10.16 сантиметров
6 дюймов - 15.24 сантиметров
8 дюймов - 20.32 сантиметров
10 дюймов - 25.4 сантиметров
12 дюймов - 30.48 сантиметров
*/

/*int x;
	double itog;
	itog = 0;
	x = 2;
	while (x <= 12)
	{
		itog = x * 25.4 / 10;
		cout << x << " дюймов - " << itog << " сантиметров" << endl;
		x += 2;
	}	*/


	/*int x = 2;
		double itog = 0;
		do
		{
			itog = x * 25.4 / 10;
			cout << x << " дюймов - " << itog << " сантиметров" << endl;
			x += 2;
		} while (x <= 12);*/
